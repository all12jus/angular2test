export class Product {
  // let name: string | undefined;
  // public id: string = "";
  constructor(public id: string, public imageUrl: string, public title: string, public subtitle: string, public body: string) {
    // if (name === undefined || name === ""){
    //   name = "Unknown"
    // }
    // if (body === undefined || body === ""){
    //   body = "Unknown"
    // }
    // if (id === undefined || id === ""){
    //   id = "Unknown"
    // }
    // this.id = name.toLowerCase()
  }
}
